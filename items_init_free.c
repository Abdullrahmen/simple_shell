#include "main.h"

/**
* init_env - Initialize an Item struct environment from char **
* note:the pointer must has a value even if it points to an empty Item
* @_env: the char ** env
* Return: pointer to the first node of Item
*/
Item *init_env(char **_env)
{}
